---
solodeveling_schema: 1
id: EVIDENCE-0033
work_id: WORK-0031
date: 2026-08-14
---

# Fork Examples comparison-lab evidence

## Implemented comparison contract

- Tracefield changes from an interchangeable observability promise to an
  incident-led trace workspace with sample latency and trace relationships.
- Northline changes from an equal-weight card mosaic to an exception-led control
  surface with fictional route state, ETA, and priority evidence.
- Syntax changes from course marketing to an exercise-led workspace with sample
  lesson sequence, code, and progress evidence.
- A global control changes all three examples; each comparison also has independent
  controls and a live status label. `?view=after` restores every result state.

## Automated checks

- `node --check site/examples/examples.js`: passed.
- `python -X utf8 scripts/validate_release.py --release-version
  1.3.0-devmonstr.1`: passed.
- `git diff --check`: passed before lifecycle records were added.

## Rendered Browser observations

- Widths observed: 360, 768, 1440, and 1600 px.
- No unintended horizontal overflow was observed at any tested width.
- Hero title occupied 84 px at 360×800 and 189 px at 1440×900; the primary
  comparison controls and product evidence remained in the surrounding page flow.
- Mobile comparison buttons rendered at 48 px tall.
- Global pointer activation changed all stages and ARIA pressed states to After.
- Per-example activation changed only the selected case and cleared both global
  pressed states when the three results no longer matched.
- Keyboard Tab exposed a 3 px visible focus outline; Space activated the global
  With Gridgeist state.
- Hidden baselines reported `aria-hidden=true`; visible results reported
  `aria-hidden=false`.
- Tracefield incident evidence, Northline priority route state, and Syntax lesson
  code were visibly inspected on desktop and mobile.
- Browser logs contained no warning or error entries.

## Boundaries

- These interfaces and all displayed operational or lesson data are illustrative,
  sample, or fictional; they are not client work or outcome evidence.
- Real 200% browser zoom and assistive-technology testing were not performed.
- Production deployment remains a separate verification boundary from the local
  rendered evidence recorded here.
